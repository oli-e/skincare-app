version: '3'
services:
  frontend:
      build: 
        context: .
        dockerfile: Dockerfile
      container_name: frontend
      ports:
        - 3000:3000
      command: "npm start"
  backend:
      build:
        context: .
        dockerfile: Dockerfile
      container_name: backend
      ports:
        - 9000:9000
      command: "sbt run"
